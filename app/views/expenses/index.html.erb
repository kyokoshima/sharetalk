<p id="notice"><%= notice %></p>
<div class="header">
  <h1>ShareTalk</h1>
</div>

<p></p>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @expenses.each do |expense| %>
      <tr class="table-index">
        <th><%= expense.title %></th>
        <td><%= link_to '詳細', expense %></td>
        <td><%= link_to '編集', edit_expense_path(expense) %></td>
        <td><%= link_to '削除', expense, method: :delete, data: { confirm: '削除してもよろしいですか？' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<div id="new-expense"><%= link_to 'New Expense', new_expense_path %></div>
