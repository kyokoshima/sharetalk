<%= form_for(expense) do |f| %>
  <% if expense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expense.errors.count, "error") %> prohibited this expense from being saved:</h2>

      <ul>
      <% expense.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

  <%= fields_for :costs do |cost| %>
    <div class="field">
      <%= cost.label :subject %>
      <%= cost.text_field :subject %>
    </div>

    <div class="field">
      <%= cost.label :cost %>
      <%= cost.number_field :cost %>
    </div>

    <div class="field">
      <%= cost.label :detail %>
      <%= cost.text_area :detail %>
    </div>

    <div class="actions">
      <%= cost.submit %>
    </div>
  <% end %>
<% end %>
