<%= form_for(timeline) do |f| %>
  <% if timeline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(timeline.errors.count, "error") %> prohibited this timeline from being saved:</h2>
    </div>
  <% end %>

  <div class="field">
    <P>
    <%= f.label :タイムライン %>
    </P>
    <%= f.text_area :content, placeholder: "今日の予定は？" %>
  </div>

  <div class="actions">
    <%= flash[:notice] %>
    <%= f.submit(['投稿']) %>
  </div>
<% end %>

